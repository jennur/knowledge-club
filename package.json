{
  "name": "knowledge-hub",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run build-css --prefix ./app/views && npm run build --prefix ./app/views && node server.js",
    "server-only": "node server.js",
    "install-client": "npm i --prefix ./app/views",
    "client-only": "npm run dev --prefix ./app/views"
  },
  "author": "janivi",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.21.4"
  }
}
