export default function highlightSort(a, b){
  // /*
  // Inputs: 
  // a - highlight 1 to compare
  // b - highlight 2 to compare

  // We want to sort the highlights by descending starting loc. IF the starting loc is the same then we want to have highlights that are smaller to be rated higher.

  // Outputs:
  //  1 -> a is greater than b
  //  -1 -> a is less than b 
  //  0 -> they are equal
  // */
  // if ( a.startloc > b.startloc ) return -1;
  // else if ( a.startloc < b.startloc ) return 1;
  // else{
  //   if(a.endloc<b.endloc){
  //     return 1;
  //   }
  //   else if(a.endloc>b.endloc){
  //     return -1;
  //   }
  //   else{
  //     return 0;
  //   }
  // }
}